server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/text2sql_app?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root

logging:
  level:
    com.text2sql: DEBUG
    org.springframework.web: DEBUG

ai:
  dashscope:
    api-key: your-api-key-here
